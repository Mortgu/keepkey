openapi: 3.1.0
info:
  title: Prisma API
  description: ""
  version: 1.0.0
paths: {}
components:
  schemas:
    Contract:
      type: object
      properties:
        id:
          type: string
        key:
          type: string
        translations:
          type: array
          items:
            $ref: "#/components/schemas/ContractTranslation"
        orderPositions:
          type: array
          items:
            $ref: "#/components/schemas/OrderPosition"
        offerPositions:
          type: array
          items:
            $ref: "#/components/schemas/OfferPosition"
        tariffs:
          type: array
          items:
            $ref: "#/components/schemas/Tariff"
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - key
        - translations
        - orderPositions
        - offerPositions
        - tariffs
    ContractTranslation:
      type: object
      properties:
        contractId:
          type: string
        language:
          $ref: "#/components/schemas/Language"
        name:
          type: string
        features:
          type: array
          items:
            type: string
        table:
          type: string
        contract:
          $ref: "#/components/schemas/Contract"
      required:
        - contractId
        - language
        - name
        - features
        - table
        - contract
    Customer:
      type: object
      properties:
        id:
          type: string
        customerId:
          type: string
        companyName:
          type: string
        email:
          type: string
        invoiceEmail:
          type: string
        street:
          type: string
        city:
          type: string
        plz:
          type: string
        phone:
          type: string
        language:
          $ref: "#/components/schemas/Language"
        country:
          type: string
        currency:
          $ref: "#/components/schemas/Currency"
        salutation:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        contactPersons:
          type: array
          items:
            $ref: "#/components/schemas/ContactPerson"
        offers:
          type: array
          items:
            $ref: "#/components/schemas/Offer"
        orders:
          type: array
          items:
            $ref: "#/components/schemas/Order"
        users:
          type: array
          items:
            $ref: "#/components/schemas/User"
        tariffCellCustomerPrice:
          type: array
          items:
            $ref: "#/components/schemas/TariffCellCustomerPrice"
      required:
        - id
        - companyName
        - createdAt
        - updatedAt
        - contactPersons
        - offers
        - orders
        - users
        - tariffCellCustomerPrice
    ContactPerson:
      type: object
      properties:
        id:
          type: string
        customerId:
          type: string
        customer:
          $ref: "#/components/schemas/Customer"
        salutation:
          type: string
        firstName:
          type: string
        lastName:
          type: string
        email:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        offers:
          type: array
          items:
            $ref: "#/components/schemas/Offer"
        orders:
          type: array
          items:
            $ref: "#/components/schemas/Order"
      required:
        - id
        - customerId
        - customer
        - salutation
        - firstName
        - lastName
        - createdAt
        - updatedAt
        - offers
        - orders
    Document:
      type: object
      properties:
        id:
          type: string
        displayName:
          type: string
        format:
          $ref: "#/components/schemas/DocumentFormat"
        status:
          $ref: "#/components/schemas/DocumentStatus"
        path:
          type: string
        taskId:
          type: string
        task:
          $ref: "#/components/schemas/Task"
        offerDocument:
          $ref: "#/components/schemas/OfferDocument"
        orderDocument:
          $ref: "#/components/schemas/OrderDocument"
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - format
        - status
        - createdAt
    Offer:
      type: object
      properties:
        id:
          type: string
        supplier:
          $ref: "#/components/schemas/Supplier"
        supplierId:
          type: string
        customer:
          $ref: "#/components/schemas/Customer"
        customerId:
          type: string
        customerContactPerson:
          $ref: "#/components/schemas/ContactPerson"
        contactPersonId:
          type: string
        user:
          $ref: "#/components/schemas/User"
        userId:
          type: string
        language:
          $ref: "#/components/schemas/Language"
        quoteId:
          type: string
        paymentTerm:
          type: string
        date:
          type: string
          format: date-time
        validUntil:
          type: string
          format: date-time
        requestFrom:
          type: string
          format: date-time
        offerFlatRates:
          type: array
          items:
            $ref: "#/components/schemas/OfferFlatRate"
        offerPositions:
          type: array
          items:
            $ref: "#/components/schemas/OfferPosition"
        offerDocuments:
          type: array
          items:
            $ref: "#/components/schemas/OfferDocument"
        orders:
          $ref: "#/components/schemas/Order"
        isReserved:
          type: boolean
        reservationTaskId:
          type: string
        reservationTask:
          $ref: "#/components/schemas/Task"
        net_amount:
          type: integer
          format: int32
        version:
          type: integer
          format: int32
        revisions:
          type: array
          items:
            $ref: "#/components/schemas/OfferRevision"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - customer
        - customerId
        - customerContactPerson
        - contactPersonId
        - user
        - userId
        - language
        - quoteId
        - paymentTerm
        - date
        - offerFlatRates
        - offerPositions
        - offerDocuments
        - isReserved
        - net_amount
        - version
        - revisions
        - createdAt
        - updatedAt
    OfferRevision:
      type: object
      properties:
        id:
          type: string
        offer:
          $ref: "#/components/schemas/Offer"
        offerId:
          type: string
        version:
          type: integer
          format: int32
        snapshot:
          type: object
        changedBy:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - offer
        - offerId
        - version
        - snapshot
        - createdAt
    OfferPosition:
      type: object
      properties:
        id:
          type: string
        offer:
          $ref: "#/components/schemas/Offer"
        offerId:
          type: string
        product:
          $ref: "#/components/schemas/Product"
        productId:
          type: string
        contract:
          $ref: "#/components/schemas/Contract"
        contractId:
          type: string
        duration_months:
          type: integer
          format: int32
        quantity:
          type: integer
          format: int32
        optional:
          type: boolean
        total_cents:
          type: integer
          format: int32
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - offer
        - offerId
        - product
        - productId
        - contract
        - contractId
        - duration_months
        - quantity
        - total_cents
        - createdAt
        - updatedAt
    OfferFlatRate:
      type: object
      properties:
        id:
          type: string
        flatRate:
          $ref: "#/components/schemas/FlatRate"
        flatRateId:
          type: string
        offer:
          $ref: "#/components/schemas/Offer"
        offerId:
          type: string
        quantity:
          type: integer
          format: int32
        total_cents:
          type: integer
          format: int32
      required:
        - id
        - flatRate
        - flatRateId
        - offer
        - offerId
        - quantity
        - total_cents
    OfferDocument:
      type: object
      properties:
        id:
          type: string
        version:
          type: integer
          format: int32
        isCurrent:
          type: boolean
        document:
          $ref: "#/components/schemas/Document"
        documentId:
          type: string
        offer:
          $ref: "#/components/schemas/Offer"
        offerId:
          type: string
      required:
        - id
        - version
        - isCurrent
        - document
        - documentId
        - offer
        - offerId
    Order:
      type: object
      properties:
        id:
          type: string
        supplier:
          $ref: "#/components/schemas/Supplier"
        supplierId:
          type: string
        customer:
          $ref: "#/components/schemas/Customer"
        customerId:
          type: string
        customerContactPerson:
          $ref: "#/components/schemas/ContactPerson"
        contactPersonId:
          type: string
        employee:
          $ref: "#/components/schemas/User"
        employeeId:
          type: string
        offer:
          $ref: "#/components/schemas/Offer"
        offerId:
          type: string
        orderId:
          type: string
        paymentTerm:
          type: string
        date:
          type: string
          format: date-time
        validUntil:
          type: string
          format: date-time
        requestFrom:
          type: string
          format: date-time
        orderPositions:
          type: array
          items:
            $ref: "#/components/schemas/OrderPosition"
        flatRates:
          type: array
          items:
            $ref: "#/components/schemas/OrderFlatRate"
        orderDocuments:
          type: array
          items:
            $ref: "#/components/schemas/OrderDocument"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - customer
        - customerId
        - customerContactPerson
        - contactPersonId
        - employee
        - employeeId
        - offer
        - offerId
        - orderId
        - paymentTerm
        - date
        - orderPositions
        - flatRates
        - orderDocuments
        - createdAt
        - updatedAt
    OrderPosition:
      type: object
      properties:
        id:
          type: string
        order:
          $ref: "#/components/schemas/Order"
        orderId:
          type: string
        product:
          $ref: "#/components/schemas/Product"
        productId:
          type: string
        contract:
          $ref: "#/components/schemas/Contract"
        contractId:
          type: string
        duration_months:
          type: integer
          format: int32
        quantity:
          type: integer
          format: int32
        optional:
          type: boolean
        total_cents:
          type: integer
          format: int32
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - order
        - orderId
        - product
        - productId
        - contract
        - contractId
        - duration_months
        - quantity
        - total_cents
        - createdAt
    OrderFlatRate:
      type: object
      properties:
        id:
          type: string
        flatRate:
          $ref: "#/components/schemas/FlatRate"
        flatRateId:
          type: string
        order:
          $ref: "#/components/schemas/Order"
        orderId:
          type: string
        quantity:
          type: integer
          format: int32
        total_cents:
          type: integer
          format: int32
      required:
        - id
        - flatRate
        - flatRateId
        - order
        - orderId
        - quantity
        - total_cents
    OrderDocument:
      type: object
      properties:
        id:
          type: string
        version:
          type: integer
          format: int32
        isCurrent:
          type: boolean
        document:
          $ref: "#/components/schemas/Document"
        documentId:
          type: string
        order:
          $ref: "#/components/schemas/Order"
        orderId:
          type: string
      required:
        - id
        - version
        - isCurrent
        - document
        - documentId
        - order
        - orderId
    Product:
      type: object
      properties:
        id:
          type: string
        key:
          type: string
        translations:
          type: array
          items:
            $ref: "#/components/schemas/ProductTranslation"
        orderPositions:
          type: array
          items:
            $ref: "#/components/schemas/OrderPosition"
        offerPositions:
          type: array
          items:
            $ref: "#/components/schemas/OfferPosition"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        tariffs:
          type: array
          items:
            $ref: "#/components/schemas/Tariff"
      required:
        - id
        - key
        - translations
        - orderPositions
        - offerPositions
        - createdAt
        - updatedAt
        - tariffs
    ProductTranslation:
      type: object
      properties:
        productId:
          type: string
        language:
          $ref: "#/components/schemas/Language"
        name:
          type: string
        description:
          type: string
        table:
          type: string
        product:
          $ref: "#/components/schemas/Product"
      required:
        - productId
        - language
        - name
        - product
    User:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        salutation:
          type: string
        firstName:
          type: string
        lastName:
          type: string
        phone:
          type: string
        email:
          type: string
        emailVerified:
          type: boolean
        image:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        sessions:
          type: array
          items:
            $ref: "#/components/schemas/Session"
        accounts:
          type: array
          items:
            $ref: "#/components/schemas/Account"
        role:
          type: string
        banned:
          type: boolean
        banReason:
          type: string
        banExpires:
          type: string
          format: date-time
        customer:
          $ref: "#/components/schemas/Customer"
        customerId:
          type: string
        offers:
          type: array
          items:
            $ref: "#/components/schemas/Offer"
        orders:
          type: array
          items:
            $ref: "#/components/schemas/Order"
      required:
        - id
        - name
        - salutation
        - firstName
        - lastName
        - email
        - emailVerified
        - createdAt
        - updatedAt
        - sessions
        - accounts
        - offers
        - orders
    Session:
      type: object
      properties:
        id:
          type: string
        expiresAt:
          type: string
          format: date-time
        token:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        ipAddress:
          type: string
        userAgent:
          type: string
        userId:
          type: string
        user:
          $ref: "#/components/schemas/User"
        impersonatedBy:
          type: string
      required:
        - id
        - expiresAt
        - token
        - createdAt
        - updatedAt
        - userId
        - user
    Account:
      type: object
      properties:
        id:
          type: string
        accountId:
          type: string
        providerId:
          type: string
        userId:
          type: string
        user:
          $ref: "#/components/schemas/User"
        accessToken:
          type: string
        refreshToken:
          type: string
        idToken:
          type: string
        accessTokenExpiresAt:
          type: string
          format: date-time
        refreshTokenExpiresAt:
          type: string
          format: date-time
        scope:
          type: string
        password:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - accountId
        - providerId
        - userId
        - user
        - createdAt
        - updatedAt
    Verification:
      type: object
      properties:
        id:
          type: string
        identifier:
          type: string
        value:
          type: string
        expiresAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - identifier
        - value
        - expiresAt
    Supplier:
      type: object
      properties:
        id:
          type: string
        supplierId:
          type: string
        name:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        offers:
          type: array
          items:
            $ref: "#/components/schemas/Offer"
        orders:
          type: array
          items:
            $ref: "#/components/schemas/Order"
      required:
        - id
        - supplierId
        - name
        - createdAt
        - updatedAt
        - offers
        - orders
    FlatRate:
      type: object
      properties:
        id:
          type: string
        key:
          type: string
        total_cents:
          type: integer
          format: int32
        translations:
          type: array
          items:
            $ref: "#/components/schemas/FlatRateTranslation"
        offerFlatRates:
          type: array
          items:
            $ref: "#/components/schemas/OfferFlatRate"
        orderFlatRates:
          type: array
          items:
            $ref: "#/components/schemas/OrderFlatRate"
      required:
        - id
        - key
        - total_cents
        - translations
        - offerFlatRates
        - orderFlatRates
    FlatRateTranslation:
      type: object
      properties:
        flatRateId:
          type: string
        language:
          $ref: "#/components/schemas/Language"
        name:
          type: string
        table:
          type: string
        flatRate:
          $ref: "#/components/schemas/FlatRate"
      required:
        - flatRateId
        - language
        - name
        - table
        - flatRate
    Tariff:
      type: object
      properties:
        id:
          type: string
        product:
          $ref: "#/components/schemas/Product"
        productId:
          type: string
        contract:
          $ref: "#/components/schemas/Contract"
        contractId:
          type: string
        terms:
          type: array
          items:
            type: integer
            format: int32
        rows:
          type: array
          items:
            $ref: "#/components/schemas/TariffRow"
        validFrom:
          type: string
          format: date-time
        validTo:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - product
        - productId
        - contract
        - contractId
        - terms
        - rows
        - validFrom
        - createdAt
        - updatedAt
    TariffRow:
      type: object
      properties:
        id:
          type: string
        tariff:
          $ref: "#/components/schemas/Tariff"
        tariffId:
          type: string
        min_quantity:
          type: integer
          format: int32
        max_quantity:
          type: integer
          format: int32
        order:
          type: integer
          format: int32
        cells:
          type: array
          items:
            $ref: "#/components/schemas/TariffCell"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - tariff
        - tariffId
        - min_quantity
        - max_quantity
        - order
        - cells
        - createdAt
        - updatedAt
    TariffCell:
      type: object
      properties:
        id:
          type: string
        row:
          $ref: "#/components/schemas/TariffRow"
        rowId:
          type: string
        price:
          type: integer
          format: int32
        order:
          type: integer
          format: int32
        customerPrices:
          type: array
          items:
            $ref: "#/components/schemas/TariffCellCustomerPrice"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - row
        - rowId
        - price
        - order
        - customerPrices
        - createdAt
        - updatedAt
    TariffCellCustomerPrice:
      type: object
      properties:
        id:
          type: string
        cell:
          $ref: "#/components/schemas/TariffCell"
        cellId:
          type: string
        customer:
          $ref: "#/components/schemas/Customer"
        customerId:
          type: string
        price:
          type: integer
          format: int32
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - cell
        - cellId
        - customer
        - customerId
        - price
        - createdAt
        - updatedAt
    Task:
      type: object
      properties:
        id:
          type: string
        jobId:
          type: string
        status:
          $ref: "#/components/schemas/TaskStatus"
        target:
          $ref: "#/components/schemas/TaskTarget"
        type:
          $ref: "#/components/schemas/TaskType"
        error:
          type: string
        document:
          $ref: "#/components/schemas/Document"
        offerReservation:
          $ref: "#/components/schemas/Offer"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - status
        - target
        - type
        - createdAt
        - updatedAt
    DocumentStatus:
      type: string
      enum:
        - PENDING
        - PROCESSING
        - GENERATED
        - UPLOADING
        - UPLOADED
        - FAILED
    DocumentFormat:
      type: string
      enum:
        - PDF
        - DOCX
        - RESERVED
    Language:
      type: string
      enum:
        - DE
        - EN
    Currency:
      type: string
      enum:
        - EUR
        - RAND
        - DOLLAR
        - CHF
    TaskStatus:
      type: string
      enum:
        - COMPLETED
        - RUNNING
        - PENDING
        - FAILED
    TaskTarget:
      type: string
      enum:
        - OFFER
        - ORDER
        - RENEWAL
    TaskType:
      type: string
      enum:
        - UPLOAD
        - RESERVATION
        - GENERATION
  responses: {}
  parameters: {}
  examples: {}
  requestBodies: {}
  headers: {}
  securitySchemes: {}
  links: {}
  callbacks: {}
tags: []
servers: []
