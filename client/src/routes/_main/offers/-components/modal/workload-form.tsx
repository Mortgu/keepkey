import type { OfferProductInput } from "../modal-components/offer-product-form";

interface Props {
    currentWorkload?: OfferProductInput;
}

export default function WorkloadFormOfferModal({ currentWorkload }: Props) {
    return (
        <></>
    )
}